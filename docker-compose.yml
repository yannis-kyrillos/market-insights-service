services:
  mongodb:
    image: mongo:7
    container_name: market-insights-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: market_insights

volumes:
  mongodb_data:
